# OpenAI API Key (for GPT-4 agent provider)
OPENAI_API_KEY=

# Server Configuration
# Location for runtime state (agents, etc.). Defaults to ~/.junction
JUNCTION_HOME=~/.junction

# Listen address. Defaults to 127.0.0.1:6767
# Can be host:port, /path/to/socket, or unix:///path/to/socket
JUNCTION_LISTEN=127.0.0.1:6767
