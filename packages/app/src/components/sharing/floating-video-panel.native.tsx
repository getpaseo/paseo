/**
 * Native stub — video panel requires react-native-webrtc.
 * Install react-native-webrtc to enable on iOS/Android.
 */
export function FloatingVideoPanel(_props: { visible: boolean; onClose: () => void }) {
  return null;
}
