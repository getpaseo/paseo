/**
 * Native stub — LiveKit's React components are web-only, so the floating
 * video panel is disabled on iOS/Android for now.
 */
export function FloatingVideoPanel(_props: { visible: boolean; onClose: () => void }) {
  return null;
}
