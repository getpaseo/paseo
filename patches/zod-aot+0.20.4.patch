diff --git a/node_modules/zod-aot/dist/cli/emitter.js b/node_modules/zod-aot/dist/cli/emitter.js
index 1a4bc67..3523d58 100644
--- a/node_modules/zod-aot/dist/cli/emitter.js
+++ b/node_modules/zod-aot/dist/cli/emitter.js
@@ -2,7 +2,9 @@ import fs from "node:fs";
 import path from "node:path";
 import { FIN_DECL, generateIIFE, MK_VALIDATOR_DECL, ZOD_CONFIG_IMPORT, ZOD_MSG_DECLARATION, } from "#src/core/iife.js";
 export function generateCompiledFileContent(schemas, sourceRelPath, options) {
-    let importPath = sourceRelPath.replace(/\.[cm]?[jt]sx?$/, "");
+    let importPath = sourceRelPath.endsWith(".js")
+        ? sourceRelPath
+        : sourceRelPath.replace(/\.[cm]?[jt]sx?$/, "");
     if (!importPath.startsWith(".")) {
         importPath = `./${importPath}`;
     }
diff --git a/node_modules/zod-aot/dist/core/codegen/schemas/discriminated-union.js b/node_modules/zod-aot/dist/core/codegen/schemas/discriminated-union.js
index 5309c47..e642696 100644
--- a/node_modules/zod-aot/dist/core/codegen/schemas/discriminated-union.js
+++ b/node_modules/zod-aot/dist/core/codegen/schemas/discriminated-union.js
@@ -1,8 +1,9 @@
-import { escapeString } from "../context.js";
+import { escapeString, hasMutation } from "../context.js";
 import { emit } from "../emit.js";
 import { invalidType } from "../emit-issue.js";
 export function slowDiscriminatedUnion(ir, g) {
     const discKey = escapeString(ir.discriminator);
+    const needsOutputPropagation = ir.options.some(hasMutation);
     let code = emit `
     if(typeof ${g.input}!=="object"||${g.input}===null||Array.isArray(${g.input})){
       ${invalidType(g, "object")}
@@ -14,6 +15,7 @@ export function slowDiscriminatedUnion(ir, g) {
         code += emit `
       case ${escapeString(value)}:
         ${g.visit(option, { input: objVar, output: objVar })}
+        ${needsOutputPropagation ? `${g.output}=${objVar};` : ""}
         break;`;
     }
     const validValues = Object.keys(ir.mapping)
