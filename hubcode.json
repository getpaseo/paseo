{
  "worktree": {
    "setup": [
      "npm ci",
      "npm run build:daemon",
      "cp \"$HUBCODE_SOURCE_CHECKOUT_PATH/packages/server/.env\" \"$HUBCODE_WORKTREE_PATH/packages/server/.env\""
    ]
  },
  "scripts": {
    "daemon": {
      "type": "service",
      "command": "HUBCODE_LISTEN=0.0.0.0:$HUBCODE_PORT ./scripts/dev-daemon.sh"
    },
    "app": {
      "type": "service",
      "command": "cd packages/app && BROWSER=none EXPO_PUBLIC_LOCAL_DAEMON=\"${HUBCODE_SERVICE_DAEMON_URL#http://}\" npx expo start --port $HUBCODE_PORT"
    },
    "typecheck": {
      "command": "npm run typecheck"
    }
  }
}
