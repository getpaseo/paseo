pre-commit:
  jobs:
    - name: format
      run: npm run format
      stage_fixed: true
    - name: checks
      group:
        parallel: true
        jobs:
          - name: lint
            run: npm run lint
          - name: typecheck
            run: npm run typecheck
